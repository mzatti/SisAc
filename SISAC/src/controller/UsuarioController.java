package controller;

import model.Usuario;

public class UsuarioController {
	
	/*private void botaoEntrar(java.awt.event.ActionEvent evt) { 
		Usuario u = new Usuario(); 
		u.setId(jTextFieldId.getText());  
		u.setSenha(jTextFieldSenha.getText()); 
		UsuarioDAO.getInstance().merge(u); 
		clearFields(); JOptionPane.showMessageDialog(this, "Salvo com sucesso !"); } 

	*/	
}
